## Bronze Layer – PySpark Code

This folder contains screenshots of PySpark code used for **raw data ingestion**.

### Key Operations

- Loading raw datasets into Spark DataFrames
- Writing data into Bronze Delta tables
- Preserving raw structure without transformations

The Bronze layer serves as the raw and immutable source for downstream processing.
